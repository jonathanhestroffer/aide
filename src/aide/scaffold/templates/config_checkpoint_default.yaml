enabled: true
monitor: val_loss
mode: min
save_last: true
save_top_k: 3
every_n_epochs: null
every_n_train_steps: null
dirpath: ${hydra:runtime.output_dir}/checkpoints
filename: "{epoch}-{step}-{val_loss:.4f}"
